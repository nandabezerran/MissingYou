<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smile.missingyou">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:logo="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GuiaActivity" />
        <activity android:name=".CadastroUsuarioActivity" />
        <activity android:name=".CadastroCampanhaActivity" />
        <activity android:name=".AtualizarUsuarioActivity" />
        <activity android:name=".AtualizarCampanhaActivity"/>
        <activity android:name=".AjudarCampanhaActivity"/>
        <activity android:name=".AjudarEstaActivity"/>
        <activity android:name=".AjudarViuActivity"/>
        <activity android:name=".AjudarDelegaciaActivity"/>
        <activity android:name=".PopUpCPF"
            android:theme="@style/AppTheme.CustomTheme"
        />
        <activity android:name=".PopUpAjudaEnviada"
            android:theme="@style/AppTheme.CustomTheme"
           />
        <activity android:name=".PopUpAjudaRecebida"
            android:theme="@style/AppTheme.CustomTheme"
            />
    </application>

</manifest>